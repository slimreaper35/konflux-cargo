# konflux-cargo

This repository contains a minimal example of a Rust project with a mismatch between the `Cargo.toml`
file and the `Cargo.lock` file.

## Cargo.toml dependencies

- rayon
- serde

## Cargo.lock dependencies

- serde
